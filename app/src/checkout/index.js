/**
 * Checkout frontend entry point.
 *
 * The legacy main.js is loaded as-is (behavior identical) while its modules
 * are extracted incrementally into this directory. New checkout modules
 * should be created here and imported below.
 *
 * @since 6.0.0
 */
import './main.js';
